# 设计文档 (Design Document)

**中文** | [English](./docs/en/DESIGN.md)

本文档说明这套知识图谱系统**为什么这样设计**，以及关键决策的依据。如果你想直接动手用，看 [`DEVELOPMENT.md`](./DEVELOPMENT.md)。

> v2 变更：新增**治理层**（MCP 唯一出入口 + 写入强校验 + 审计日志 + 防绕行 hook）。
> 数据 schema 与 v1 兼容，变化的是"怎么读写"，见 §5。

---

## 1. 要解决的问题

### 1.1 AI 在不熟悉代码库里"瞎找"非常贵

一个简单的请求"这个公会系统的成员上限在哪改"，AI 如果没有图谱可能要：

1. 多次 grep 找文件
2. 读 3-4 个文件全文
3. 推断哪个是入口
4. 还可能基于错误假设写代码

token 消耗能差 5-10 倍，且经常漏掉跨模块联动。

### 1.2 传统 wiki 解决不了这个问题

业界常见的自动生成 wiki（DeepWiki / RepoWiki 类）会生成 5-10 万行的项目文档，覆盖每个模块的详细描述。问题：

- 体量太大，AI 读完单份就花 5-15k token
- 信息密度低（30-40% 是模板化 boilerplate）
- 维护成本高，自动生成跑不动就漂移
- 抽象层级不对——给的是"源码二次描述"，AI 其实更需要"在哪改 + 别漏什么"

### 1.3 真实需求是导航，不是百科全书

AI 在做新需求时真正需要的是三类信息：

1. **概念入口**：这个系统是什么、核心数据是什么、关键约定是什么
2. **关联系统**：改这个会牵涉哪些其他系统
3. **代码定位**：直接告诉我去 view 哪些文件，省掉 grep

第 1 类是叙述性的，第 2、3 类是结构化的。**单一格式无法两者兼顾，所以必须分层。**

### 1.4 v1 的教训：图谱自己会烂掉（v2 新增）

v1 靠"AI 直接编辑 JSON + 文档里写规范 + 人工跑校验脚本"维护图谱。真实项目跑了
一段时间后暴露的问题：

- **记录漂移**：AI 写 entry 时凭记忆填代码路径，写错了没有任何机制拦截。
  错误路径进了图谱，下次查询的 AI 被误导，浪费的 token 比没有图谱还多
- **校验靠自觉**：validate.py 需要人记得手动跑。事实是没人记得，
  错误累积到肉眼可见才被发现
- **无法追溯**：图谱文件被多次会话、多个工具反复编辑，出了错不知道是哪次任务、
  基于什么理由改的；也不知道哪些 entry 真正被用到，反馈迭代无从谈起

结论：**给 AI 的规范如果只写在文档里，等于没有规范。** 规范必须变成机制——
这就是 v2 治理层的由来。

---

## 2. 核心心智模型：字典 + 索引 + 管理员

把这套系统理解为**一本带索引、有管理员的字典**。

```
+---------------------------+         +---------------------------+
|    MD 字典正文（叙述）      |         |    JSON 索引页（结构）    |
|---------------------------|         |---------------------------|
| 公会系统是一个 ...         |  <--->  | guild:                    |
| 关键约定：改成员后必须 ...   |         |   related: [mail, log]    |
| 踩坑：跨服同步的边界 ...    |         |   code: GuildManager.java |
+---------------------------+         +---------------------------+
        给人理解 why                       给 AI 快速跳转
        可直接编辑                         只能通过 MCP 出入口读写
                                                 |
                                        +---------------------------+
                                        |   MCP 出入口（管理员）     |
                                        |---------------------------|
                                        | 写入强校验（路径必须存在）  |
                                        | changelog / querylog      |
                                        | hook 拦截绕行             |
                                        +---------------------------+
```

AI 工作流：

1. **kg_query 找入口**：关键词一次拿到相关 entry 的代码路径、踩坑、出边入边
2. **MD 找概念（可选）**：entry 的 doc 存在时读它，对齐心智模型
3. **view 代码**：跳过所有 grep，直接读 JSON 给的文件
4. **回写经验**：任务完成后经用户确认，用写工具把踩坑/验证结果记回图谱

---

## 3. 四个组件的职责分离

### 3.1 MD 字典（叙述层）

**写什么**：

- 系统/功能的概念描述（2-3 段）
- 核心数据模型（结构 + 用途）
- 关键约定（saveToDB 模式、跨服规则、隐式契约）
- 典型场景（什么时候 AI 应该来这个 entry）
- 已知踩坑（具体的 bug 历史）

**不写什么**：

- 详细的方法调用流程（看代码就知道）
- 完整的数据流图（除非真的核心）
- 源码级实现细节
- 重复 JSON 里已有的依赖列表

**长度建议**：每个 entry 100-300 行。超过 500 行说明混入了源码细节，应该砍。

MD 是给人（和深入阅读的 AI）的叙述层，**不受出入口管制，可直接编辑**。

### 3.2 JSON 索引（结构层）

**包含什么**：

- 节点（系统/功能/概念）的元数据：类型、入口文件、对应的 MD entry
- 节点之间的 `related`：关联系统 + 一句话描述上下文
- 持久化注意点：哪些字段改了必须 saveToDB
- 代码路径：manager 类、data 类、dao 类、message 类

**不包含什么**：

- 概念解释（在 MD 里）
- why 的论证（在 MD 里）
- 详细业务规则（在 MD 里）
- 行级源码引用（会漂移，不维护）

**测试边界对不对**：删掉 MD，AI 还能基本搞清楚关系吗？如果不能，说明 JSON 不完整，关键事实漏在 MD 里了。

### 3.3 MCP 出入口（治理层，v2 新增）

**唯一的读写通道**。一个纯标准库的 stdio MCP server（`kg_mcp_server.py`），
Claude Code / Codex 等支持 MCP 的工具通用。职责：

- **查询**：`kg_query` 返回"一次够用"的信息（entry + 出边 + 入边 + 踩坑），
  免去 AI 挨个读 JSON 文件；每次查询记 `querylog.jsonl`
- **写入**：所有变更操作（加 entry/边/踩坑、改字段、建域）经过 schema 强校验——
  code 路径必须真实存在、related 目标必须存在、reason 必填——校验不过拒绝写入；
  写入成功自动重建反向索引、追加 `changelog.jsonl`
- **防绕行**：Claude Code 侧配 PreToolUse hook，直接 Edit/Write 图谱 JSON 会被拦截
  并提示改用 MCP 工具

书写规范不再是文档里的一段话，而是**工具的参数 schema 和拒绝逻辑**——AI 每次调用
都会看到，写错了当场被拒。

### 3.4 HTML 可视化层（可选）

**给人用，不给 AI 用。**

人无法在大型 JSON 里肉眼审计关系是否正确。HTML viewer 用 graph 渲染节点 + 连线，让人能：

- 一目了然看到整个项目的关系结构
- 点击节点查看入边/出边、踩坑、代码路径

只读即可，不要做编辑器——编辑走 MCP 工具，保证日志和校验不被绕过。

---

## 4. Schema 设计原则

### 4.1 简化优先

我们做了几个反直觉的决定，都是为了**降低维护成本**：

| 决策 | 反直觉点 | 真实原因 |
|---|---|---|
| 没有 edge type（calls/mutates/syncs） | 看起来损失语义 | 1 个月后 30 种类型谁都用不利索；用 `context` 一句话描述足够 |
| 没有 source_line 引用 | 看起来失去精度 | line 必然漂移；class 路径相对稳定，够用 |
| 只有两态 confidence | 看起来粗糙 | `draft/verified` 二态决策清晰；三态会出现"模糊地带" |
| 没有 graph database | 看起来不够"工程" | 关系总数 < 100 时 JSON 完全够；上 graph DB 是过度工程 |

### 4.2 机器可验证 → 进 JSON；解释性 → 进 MD

这是**唯一的硬规则**。例：

| 信息 | 进哪里 | 为什么 |
|---|---|---|
| GuildManager 调用 MailManager.sendGuildMail() | JSON | 可 grep 验证 |
| 公会-邮件之间的耦合应该用事件而不是直接调用 | MD | 设计观点 |
| Guild.storeData 改了必须 saveToDB | JSON（persistence_pitfalls） | 可枚举 |
| 这个约定的历史和原因 | MD | 解释性 |

### 4.3 JSON 永远是 source of truth（图谱内部）

MD 引用 JSON，**不重复列出**。例：

❌ 错的：MD 里写"公会依赖：邮件、日志、跨服" + JSON 里也写一遍 → 双源头同步问题
✓ 对的：MD 里写"详细依赖见 guild entry 的 related" → JSON 是唯一权威

（图谱与代码之间，**代码**永远是 source of truth——图谱只是加速器。）

### 4.4 Confidence 只用作"行为提示"，不用作"过滤器"

**保留**：用作 AI 行为提示（遇到 `draft` 边时多 grep 一步验证）
**不保留**：用作"任务关键度 × confidence 等级"的复杂查询逻辑

理由：多数任务关键度差不多，强行分级实际收益小。简单的"draft → 多验证一步"规则就够。

---

## 5. 治理层设计（v2 新增）

### 5.1 出入口收敛的推导

漂移的根因链条：

```
AI 直接编辑 JSON
  → 写入时无校验（文档规范约束不了生成过程）
  → 错误静默进入图谱
  → 校验靠人工跑（没人跑）
  → 错误被下一次查询消费，误导放大
  → 无日志，无法定位是哪次改动引入
```

切断链条的最小干预点是**写入**：只要所有写入都过一道强校验 + 留一条日志，
后面的环节全部不攻自破。所以设计为"唯一出入口"，而不是"更勤快地跑校验"。

### 5.2 校验分两档

- **写入时（强）**：针对本次变更的 entry——code 路径必须真实存在于代码库、
  related 目标必须已存在、type/confidence 枚举合法、reason 必填。
  **错误直接拒绝写入**，AI 当场看到原因并修正
- **全量校验（宽）**：已有存量数据的代码路径漂移只报 WARNING（代码重构导致的
  历史漂移不应阻塞新写入），悬空引用报 ERROR

为什么写入从严、存量从宽：新写入的信息 AI 刚刚接触过代码，路径必须是对的；
存量漂移是代码演化的自然结果，靠"谁发现谁顺手修"（`kg_update_entry` 修正时
同样强校验新路径）。

### 5.3 双日志的用途

- `changelog.jsonl`：每次写操作记录谁/何时/什么操作/改动前的值/**reason**。
  reason 必填是刻意的——它回答"这条记录是哪个任务、基于什么发现写进来的"，
  漂移追溯和 code review 都靠它
- `querylog.jsonl`：每次查询记录关键词和命中。它回答"图谱有没有被用、
  哪些 entry 值得维护"——这是 §8 退出标准的数据来源，v1 里这只能靠感觉

### 5.4 为什么 hook 仍然必要

MCP 只是"提供了更好的路"，AI 在长上下文里仍可能忘掉约定、图省事直接改文件。
PreToolUse hook 把约定变成硬约束。设计细节：

- 只拦图谱 JSON（判定：文件名匹配 + 所在目录含 graph.json），`entries/*.md` 放行
- **fail-open**：hook 自身出错时放行，保证 hook 故障不阻塞正常开发
- 逃生口 `KG_ALLOW_DIRECT_EDIT=1`：人工批量修复场景用，改完跑校验

Codex 没有 hook 机制，该侧靠 MCP + AGENTS.md 约定（弱一档，但只要走 MCP 写入，
校验和日志同样生效）。

---

## 6. 关键决策记录 (ADR)

### ADR-001：为什么不用现成的图数据库（Neo4j 等）

- 关系总数预期 < 200 条，JSON 完全 cover
- 图数据库引入运维负担（部署、备份、版本控制）
- AI 读 JSON 比查图数据库省 token
- Git diff 友好性远高于二进制图数据库

如果项目规模膨胀到关系数 > 1000，再考虑迁移。**不要预防性引入复杂度。**

### ADR-002：为什么 confidence 不分三态

`lo / md / hi` 三态时：

- `md` 是什么状态？"看过一半"？"基本认可"？语义模糊
- 决策成本变高（每条 edge 都要想"算 md 还是 hi"）
- 没有清晰的升级触发条件

`draft / verified` 二态：

- 决策清晰：要么验证过，要么没有
- 升级条件明确：AI 在实际任务中用过且确认正确 → `kg_verify_edge`

### ADR-003：为什么不写 edge type

最初草案有 `calls / mutates / triggers / syncs / notifies / persists_via` 等多种类型。后来砍掉，原因：

- AI 读 `context` 字段（一句话自然语言）能推断语义
- 每多一种类型就多一种维护负担
- 类型边界不清晰（"call 同时 mutate" 怎么算？）
- 类型清单会一直膨胀（每次新场景都想加新类型）

如果某个项目真的需要细分，用 `x_` 前缀扩展字段标，而不是引入全局类型枚举。

### ADR-004：为什么 HTML 可视化只做只读

编辑器是最容易过度投入的点（2-3 周工作量），且会绕过治理层（无校验、无日志）。
编辑统一走 MCP 工具；viewer 只负责让人看清结构（~1 天，cytoscape.js）。

### ADR-005：为什么出入口用 MCP 而不是 CLI（v2）

两者都能做"唯一写入口"，选 MCP 因为：

- **参数校验前置**：MCP tool 的 JSON schema 在调用时就约束字段和枚举；
  CLI 方案 AI 要在命令行拼 JSON，Windows 引号转义是重灾区，拼错即脏数据
- **规范随调用可见**：工具描述里写清"什么时候用、字段怎么填"，AI 每次调用都看到，
  不依赖它记得读文档
- **跨工具通用**：Claude Code / Codex 都支持 MCP；CLI 的调用约定各工具不一
- **日志无死角**：查询和写入都过 server 进程，`querylog` 不会被"直接 Read 文件"绕过

CLI 没有消失——`validate.py` 等薄封装保留给 CI / pre-commit / 人工，
核心逻辑在 `kg_core.py` 里两边共用。

### ADR-006：为什么强校验放在写入而不是 CI（v2）

CI 校验发现问题时，写入上下文已经丢失（哪个任务、AI 当时看到了什么），
只能人工考古。写入时拒绝，AI 当场携带完整上下文修正，成本低一个数量级。
CI 校验仍保留，兜存量漂移和人工直改的底。

### ADR-007：为什么 MCP server 纯标准库实现（v2）

发行包要"30 秒装进任意项目"。引入 SDK 依赖意味着目标项目要多一步
pip install、要处理版本冲突。MCP stdio 协议本身只是换行分隔的 JSON-RPC，
标准库 150 行内实现，可移植性优先。

### ADR-008：为什么语义命中交给调用方 AI，而不是 server 内做语义检索（v2）

`kg_query` 是字面子串匹配，天然缺陷：换个说法就查不到（"卖装备换材料"匹配不到
"装备回收"）。三个候选方案：

1. server 内嵌 embedding/向量检索 → 破坏零依赖、可复现、30 秒安装
2. server 调 LLM API 判断相关性 → 引入网络/密钥/成本，且治理层不再确定性
3. **返回轻量目录，让调用方 AI 做语义判断**（采用）

方案 3 的洞察是：**调用方本来就是 LLM**——图谱规模（几十~上百 entry）的
id+中文名+摘要目录只有 1-2k token，AI 通读一遍做语义挑选，效果等价于语义检索，
成本可忽略，server 保持纯确定性。`kg_query`（快路径，字面命中直接返回）+
`kg_catalog`（慢路径，语义兜底）两段式，无命中提示会引导 AI 走慢路径而不是
直接放弃。挑选后的 `kg_get_entry` 同样计入命中统计，querylog 不失真。

图谱大到目录本身超预算（500+ entry）时，先按域取目录（`kg_catalog(domain=...)`）；
再不够才考虑引入向量检索——到那时它才配得上自己的复杂度。

### ADR-009：为什么复用推荐是"经验层"且必须人在环里（v2.4）

图谱原本只索引**事实**（代码在哪、怎么联动）。但项目里有一类问题事实层解决不了：
需求方用「描述」提需求（"玩家上线发个奖励"），若不深挖易被当新功能开发，而其实
现成的可配置能力（枚举成员）配一下就能实现。这需要图谱能做**能力复用推荐**。

关键区分：推荐是**经验**不是**事实**。事实（"代码在 X 文件"）错了 AI 读代码能自纠；
经验（"这类需求通常能用 Y"）是概率性的——它可能是**假朋友**（动作匹配、但需求多一个
该能力配不出来的限定词，如"背后受击""连续 7 天"）。若 AI 把经验当事实直接执行，会
配出看似正确、实则残缺的方案，上线才炸。

因此三条设计：

1. **经验层与事实层分离**：能力目录带 `scenarios`（供匹配）+ `reuse_note`（写能力边界，
   即配不出来的维度）。推荐命中后，AI 读 `reuse_note` 判断需求是否含超边界的限定词——
   **边界本身就是信息**，帮 AI 识别"这个不能复用"，这是防假朋友的核心。
2. **推荐永不直接变代码**：`kg_scout_reuse` 刻意**不返回可照抄的配置串**，只给能力名 +
   场景 + 边界，逼 AI 把结论**转述给人 + 给选项**，人拍板才进实现。人的决策是把"经验参考"
   转成"决定"的闸门。
3. **粗排在 server、精排在 AI**（延续 ADR-008 的分工）：server 对 name/scenarios/reuse_note
   做关键词粗排返回 top-k，避免全量目录淹没 AI；语义分级（recommend/reference）交给 AI。

**为什么不做机器交叉校验**（曾设计过"解析源码枚举比对"）：现场实验发现，AI 在开发中
**读着代码整理**事实字段时准确率很高（出错的是"凭记忆快速填"）。加个源码解析器去二次校验，
是防一个在该工作流里不发生的问题，且解析器语言绑定、脆弱。改为「AI 读源码填 + 人确认 +
`source` 溯源线索供核对」，用 D8（推荐是参考非执行）兜底事实字段的个别偏差。

**反馈闭环**：推荐后人拍板的结果（reuse/new/misunderstood）回写 `reuse_feedback.jsonl`，
聚合成采纳率并分级。经验层因此是**数据驱动**的——能发现"总被推却没人用"（scenarios 太宽）
和误推荐，据此迭代，而不是写死一次就固化。

---

## 7. 这套系统不做的事

清晰列出 anti-features，避免 scope creep：

- **不做项目百科全书**：这是导航工具，不是文档系统
- **不做源码替代**：entry 不写源码细节，看代码就知道
- **不做实时同步**：图谱可以滞后于代码 1-2 天，写入口强校验 + 全量校验兜底
- **不做严格类型系统**：edge type、precise schema 都没必要
- **不做 AI 全自动维护**：AI 通过 MCP 工具**提议**更新，人 yes/no；
  机制保证"写进去的是对的"，人保证"该不该写"
- **不替代 grep**：图谱查不到的概念，AI 立即 fallback to grep

---

## 8. 失败模式预警

诚实列出已知容易失败的地方：

### 8.1 初始化偷懒

"AI 扫一份草稿，慢慢改"——结果：1 个月后还是草稿，没有人工补充的"约定/坑/隐式契约"，价值不兑现。

**对策**：初始化分两阶段：

1. AI 扫**结构性骨架**（30 分钟，`/kg-init`，全程走 MCP 工具）
2. 人**口述约定/坑，AI 帮填**（2-4 小时一次性投入）：必须做，否则系统废。
   治理层防得住"写错"，防不住"没人写"

### 8.2 更新触发漏洞

人工触发更新听起来好，实际：改动小→懒得更新；改动大→负担重；紧急→先修了再说。

**对策**（v2 已内建大半）：

- skill 的 post-task 环节让 AI **看本次任务**给更新建议，你只 yes/no
- 写入自动校验 + changelog，更新的成本和风险都降了
- git pre-commit 跑 `validate.py --strict`，存量漂移进不了仓库

### 8.3 图谱长成另一份废文档

如果 1 周后没人用，图谱就和那 6 万行 wiki 一个下场。

**对策**：

- skill 必须明确 trigger，让 AI 主动用
- 跑 1-2 周后看 `querylog.jsonl`：哪些 entry 从来没被查过 → 删（v1 靠感觉，v2 有数据）
- 不追求完整性，追求被用

---

## 9. 总结：这套系统赌的是什么

我们赌：

1. **维护一份精炼的导航数据，价值远超维护一份庞大的描述性文档**
2. **机制强制的写入规范，能把图谱质量的下限从"取决于 AI 自觉"抬到"进来的都是对的"**
3. **AI 优先查图谱，是 token 效率提升 50%+ 的关键**

我们不赌：

1. 不赌 AI 能完全自动维护图谱（不能，人必须 yes/no）
2. 不赌图谱能 cover 一切（不能，必有 fallback to grep）
3. 不赌一次设计就能跑通（v1 → v2 本身就是真实数据迭代出来的）

**核心成功标准**：3 个月后 `querylog.jsonl` 还在增长、`changelog.jsonl` 里的 reason
还言之有物，且 AI 在用它的任务上 token 消耗 < 不用它的对照组。

如果 3 个月达不到这个标准，**应该停掉它**，不是再加复杂度抢救。

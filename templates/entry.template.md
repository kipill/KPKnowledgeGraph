# <系统名 / 功能名>

> Entry id: `<entry_id>`
> Type: system / feature / concept
> Last updated: YYYY-MM-DD

<!--
本模板对应某个 graph-<domain>.json 中的一个 entry。
（entry 属于哪个域由它所在的域文件决定,不必在此重复声明。)
长度建议 100-200 行,最多 300 行。
> 300 行说明混入了源码细节,应该砍。
不要重复域文件里已有的 related 列表(在 JSON 里查就行)。
-->

## 是什么

<!--
2-3 段概念描述:
- 这个系统/功能在项目里负责什么
- 解决什么问题
- 在整体架构里的位置

举例:
> 公会系统是玩家之间的中型组织单位,提供成员管理、共享仓库、
> 集体活动和资源池。它是社交系统的核心组成部分,与邮件、日志、
> 跨服模块紧密耦合。
>
> 公会以 Guild 聚合根承载完整生命周期数据,通过 GuildManager
> 集中处理业务逻辑。
-->

## 核心数据

<!--
列出 5-8 个核心数据结构。每条一行,格式:
- `ClassName`: 用途说明(< 30 字)

不要展开字段细节(看代码就知道)。
-->

## 关键约定

<!--
列出该系统的隐式契约和强制规则。AI 必须知道这些才不会出错。
每条尽量具体,避免空泛。

常见约定类型:
- 持久化:修改 XX 必须调持久化方法(否则重启丢数据)
- 并发/跨节点:只在主节点写,其他节点只读
- 顺序:A 必须在 B 之前调用
- 禁止:不能在 XX 情况下做 YY
-->

## 典型场景

<!--
什么样的需求 AI 应该来这个 entry。帮 AI 判断"这个任务和我相关"。
列 5-10 个具体操作场景。
-->

## 踩坑历史

<!--
记录已知 bug 历史和修复方案。这是最有价值的部分——AI 看到这里
可以避免重复掉同样的坑。

格式:
- YYYY-MM: 问题简述 → 修复要点
-->

## 设计要点（可选）

<!--
如果有重要的设计决策值得记录,写在这里。
不是每个 entry 都需要这个 section。
-->

## 相关文档（可选）

<!--
- 链接到项目内其他 MD 文档 / 设计 doc / wiki
- 不要把这些信息复制到本文件,只放链接
-->
